#ifndef TABLE_VIEW_FUSE_H
#define TABLE_VIEW_FUSE_H

#define FUSE_USE_VERSION 26  /* richiesto da <fuse.h> */

#include <fuse.h>    /* fuse_fill_dir_t, fuse_file_info */
#include <stdio.h>   /* printf */
#include <errno.h>   /* ENOENT */
#include <unistd.h>  /* pread */

int musicmeshfs_readdir(const char*, void*, fuse_fill_dir_t, off_t,
        struct fuse_file_info*);
int musicmeshfs_getattr(const char*, struct stat*);
int musicmeshfs_rename(const char*, const char*);
int musicmeshfs_chmod(const char*, mode_t);
int musicmeshfs_read(const char*, char*, size_t, off_t, struct fuse_file_info*);

// implementate per ottimizzazioni
int musicmeshfs_open(const char*, struct fuse_file_info*);
int musicmeshfs_release(const char*, struct fuse_file_info*);


#endif
